# SwiftCast 개발 현황 정리

## 📊 프로젝트 개요

**SwiftCast**: Claude와 GLM을 자유롭게 스위칭하고 사용량을 모니터링하는 데스크톱 애플리케이션

**핵심 기능**:
1. **Provider 스위칭**: Claude ↔ GLM 간 한 번의 클릭으로 전환
2. **사용량 모니터링**: Provider별 토큰, 비용, 시간 추적
3. **프록시 서버**: Claude Code가 선택된 Provider 사용

**기술 스택**:
- Backend: Rust (Tauri 2.x, axum, SQLite)
- Frontend: React + TypeScript + Vite
- 플랫폼: Windows + macOS (크로스 플랫폼)

**핵심 요구사항**:
- ✅ **크로스 플랫폼**: Windows, macOS에서 동일하게 작동
- ✅ **런타임 독립**: JRE, Node.js 등 추가 설치 불필요
- ✅ **단독 실행**: 더블클릭으로 즉시 실행
- ✅ **작은 크기**: 5-10MB (Electron 대비 1/20)

---

## ✅ 완성된 기능

### 1. 백엔드 (Rust) - 100% 완성

#### 프록시 서버 (`src-tauri/src/proxy/`)
- ✅ axum 기반 HTTP 프록시
- ✅ SSE (Server-Sent Events) 스트리밍 지원
- ✅ 포트: 32080 (기본값, 설정으로 변경 가능)
- ✅ 자동 시작 기능 (기본 활성화)
- ✅ **OAuth 토큰 패스스루**: Anthropic API 호출 시 원본 인증 헤더 전달
- ✅ **API 키 주입**: GLM API 호출 시 저장된 키 사용
- ✅ **토큰 사용량 추적**: SSE 스트림에서 usage 정보 추출 및 DB 저장
- ✅ **테스트 완료**: Claude 및 GLM API 모두 정상 작동

#### 데이터베이스 (`src-tauri/src/storage/database.rs`)
- ✅ SQLite 기반
- ✅ 위치:
  - macOS: `~/Library/Application Support/com.swiftcast.app/data.db`
  - Windows: `%APPDATA%\com.swiftcast.app\data.db`
- ✅ 테이블:
  - `accounts`: 계정 정보
  - `usage_logs`: 사용량 로그 (요청별 토큰 추적)
  - `config`: 설정 (proxy_port, auto_start)
- ✅ 사용량 통계 기능:
  - 전체 통계 조회
  - 모델별 통계
  - 일별 통계
  - 최근 로그 조회
  - 로그 초기화

#### 계정 관리 (`src-tauri/src/models/account.rs`)
- ✅ 멀티 계정 지원
- ✅ 계정 구조: ID, Name, Base URL, Created At, Is Active
- ✅ API 키 저장: JSON 파일 (`.api_keys.json`)
- ✅ **현재 계정**: "My GLM" (GLM API)

#### Tauri Commands (`src-tauri/src/commands.rs`)
- ✅ `create_account`: 계정 생성
- ✅ `get_accounts`: 계정 목록 조회
- ✅ `get_account`: 특정 계정 조회
- ✅ `delete_account`: 계정 삭제
- ✅ `switch_account`: 계정 전환
- ✅ `get_active_account`: 활성 계정 조회
- ✅ `start_proxy`: 프록시 시작
- ✅ `stop_proxy`: 프록시 중지
- ✅ `get_proxy_status`: 프록시 상태
- ✅ `get_app_config`: 설정 조회
- ✅ `update_app_config`: 설정 업데이트
- ✅ `validate_api_key`: API 키 검증

#### 빌드
- ✅ Debug 빌드: `target/debug/swiftcast.exe` (27.6 MB)
- ✅ Release 빌드: `target/release/swiftcast.exe` (6.4 MB)

---

### 2. 프론트엔드 (React) - 90% 완성

#### 구조 (`src/`)
- ✅ `App.tsx`: 메인 앱 컴포넌트
- ✅ `components/Dashboard.tsx`: 대시보드 (프록시 상태, 계정 정보)
- ✅ `components/AccountManager.tsx`: 계정 관리 (추가/삭제/전환)
- ✅ `components/UsageMonitor.tsx`: 사용량 모니터링 (4개 탭: 개요/모델별/일별/로그)
- ✅ `components/Settings.tsx`: 설정 (포트 변경, 자동 시작 토글, 데이터 초기화)

#### 상태
- ✅ 모든 핵심 UI 컴포넌트 구현 완료
- ⚠️ 개발 서버 필요: `npm run tauri dev`

---

### 3. 설정 및 통합

#### GLM 계정
- ✅ 계정 이름: "My GLM"
- ✅ Base URL: `https://api.z.ai/api/anthropic`
- ✅ API Key: `829fa35e887947fbbc96e1e33eca965c.tPKXfhXXo2TrRs2B`
- ✅ 상태: Active

#### Claude Code 설정
- ✅ 파일:
  - macOS: `~/.claude/settings.json`
  - Windows: `%APPDATA%\Claude\settings.json`
- ✅ GLM 사용 시:
```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://localhost:32080"
  }
}
```
- ✅ Claude(Anthropic) 사용 시: settings.json 자동 삭제 (공식 API 직접 사용)

#### 프록시 테스트
- ✅ 요청: `POST http://localhost:32080/v1/messages`
- ✅ Claude(Anthropic) API: 정상 작동 (OAuth 토큰 패스스루)
- ✅ GLM API: 정상 작동 (API 키 자동 주입)
- ✅ SSE 스트리밍: 정상
- ✅ 토큰 카운트: 정상 (DB에 자동 기록)

---

### 4. 사용자 도구

#### 배치 파일 (`.bat`)
- ✅ `SwiftCast.bat`: Desktop UI 실행 (npm run tauri:dev)
- ✅ `start_proxy.bat`: 프록시만 시작
- ✅ `stop_proxy.bat`: 프록시 중지
- ✅ `switch_provider.bat`: Provider 전환 (GLM ↔ Anthropic)
- ✅ `status.bat`: 상태 확인
- ✅ `빠른실행_웹대시보드.bat`: 웹 대시보드 실행

#### 예제 스크립트 (`src-tauri/examples/`)
- ✅ `add_account.rs`: 계정 추가 CLI
- ✅ `enable_autostart.rs`: 자동 시작 활성화
- ✅ `test_proxy.rs`: 프록시 테스트

#### 웹 대시보드
- ✅ `dashboard.html`: 브라우저 기반 대시보드
- ✅ 기능: 상태 확인, 계정 정보 표시, 사용법 안내

#### 문서
- ✅ `README.txt`: 사용 가이드
- ✅ `QUICKSTART.txt`: 빠른 시작 가이드
- ✅ `여기를_읽어주세요.txt`: 실행 방법 안내
- ✅ `사용법.txt`: 한글 가이드

---

## ⚠️ 미완성 또는 개선 필요

### 1. 릴리스 빌드 미완성
**문제**: 프론트엔드가 포함된 단일 실행파일 미생성
**원인**:
- `dist` 폴더 빌드 안됨
- `tauri.conf.json`의 `frontendDist` 경로 문제
**해결 필요**: 프론트엔드 빌드 후 Tauri 번들 빌드

### 2. 세션별 모니터링 (추후 개발 가능)
**상태**: 현재 요청 단위로만 추적
- 가능 기능: Claude Code 세션별 사용량 분리
- 구현 방안: 요청 헤더에서 세션 정보 추출

---

## 🎯 현재 작동 방식

### 방법 1: Desktop UI (권장)

```bash
# 개발 모드 실행
npm run tauri dev

# 기능:
# - 프록시 자동 시작 (포트 32080)
# - 계정 관리 (추가/삭제/전환)
# - 사용량 모니터링 (모델별/일별/최근 로그)
# - 설정 변경 (포트, 자동 시작)
```

### 방법 2: 백엔드만 사용

```
1. 프록시 시작:
   target/debug/swiftcast 실행
   또는 start_proxy.bat (Windows)

2. Claude Code 재시작

3. 사용:
   Claude Code가 활성 계정(GLM/Claude)에 따라 자동 라우팅
```

### 인증 방식

```
Claude(Anthropic) 활성화 시:
  - settings.json 삭제
  - Claude Code → Anthropic API 직접 연결 (OAuth)

GLM 활성화 시:
  - settings.json에 프록시 URL 설정
  - Claude Code → SwiftCast Proxy → GLM API (API 키 주입)
```

---

## 📁 디렉토리 구조

```
C:\swiftcast\
├── src/                          # 프론트엔드 (React)
│   ├── App.tsx
│   ├── main.tsx
│   ├── components/
│   └── lib/
│
├── src-tauri/                    # 백엔드 (Rust)
│   ├── src/
│   │   ├── main.rs               # ✅ 완성
│   │   ├── commands.rs           # ✅ 완성
│   │   ├── models/
│   │   │   └── account.rs        # ✅ 완성
│   │   ├── storage/
│   │   │   ├── database.rs       # ✅ 완성
│   │   │   └── keyring.rs        # ✅ 완성
│   │   ├── proxy/
│   │   │   ├── server.rs         # ✅ 완성
│   │   │   └── handlers.rs       # ✅ 완성
│   │   └── monitor/
│   │       └── usage.rs          # ⚠️ 미사용
│   ├── examples/
│   │   ├── add_account.rs        # ✅ 완성
│   │   ├── enable_autostart.rs   # ✅ 완성
│   │   └── test_proxy.rs         # ✅ 완성
│   ├── target/
│   │   ├── debug/
│   │   │   └── swiftcast.exe     # ✅ 27.6MB
│   │   └── release/
│   │       └── swiftcast.exe     # ✅ 6.4MB
│   ├── Cargo.toml
│   └── tauri.conf.json
│
├── SwiftCast.bat                 # Desktop UI 실행
├── start_proxy.bat               # 프록시만 시작
├── stop_proxy.bat                # 프록시 중지
├── switch_provider.bat           # Provider 전환
├── status.bat                    # 상태 확인
├── 빠른실행_웹대시보드.bat        # 웹 대시보드
├── dashboard.html                # 웹 대시보드 HTML
├── README.txt                    # 사용 가이드
├── QUICKSTART.txt                # 빠른 시작
├── 여기를_읽어주세요.txt          # 실행 방법
├── 사용법.txt                    # 한글 가이드
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 🔧 데이터베이스 상태

### 위치
`C:\Users\devlo\AppData\Roaming\com.swiftcast.app\data.db`

### 현재 데이터

#### accounts 테이블
```
id: 2b44dca9-af37-4ed9-88b2-16685ed9bd41
name: My GLM
base_url: https://api.z.ai/api/anthropic
created_at: 1737185421
is_active: 1
```

#### config 테이블
```
proxy_port: 32080
auto_start: true
```

#### API Keys 파일
`C:\Users\devlo\AppData\Roaming\com.swiftcast.app\.api_keys.json`
```json
{
  "2b44dca9-af37-4ed9-88b2-16685ed9bd41": "829fa35e887947fbbc96e1e33eca965c.tPKXfhXXo2TrRs2B"
}
```

---

## 🚀 즉시 사용 가능한 방법

### 추천: Desktop UI

```bash
# macOS / Linux
npm run tauri dev

# Windows
npm run tauri dev
# 또는 SwiftCast.bat
```

**장점**:
- ✅ 프록시 자동 시작
- ✅ 직관적인 계정 관리
- ✅ 실시간 사용량 모니터링
- ✅ 포트 및 설정 변경 가능

---

## 📝 다음 단계 (선택사항)

### Option 1: 릴리스 빌드
필요한 작업:
1. Vite 프로덕션 빌드 (`npm run build`)
2. Tauri 번들 빌드 (`npm run tauri build`)
3. 단일 실행파일 생성

### Option 2: 추가 기능
- 세션별 사용량 분리 (Claude Code 세션 추적)
- 더 많은 Provider 지원 (OpenAI 등)
- 시스템 트레이 통합
- 비용 계산 기능 (토큰 → 비용 변환)
- 사용량 내보내기 (CSV/JSON)

---

## ✅ 결론

**현재 상태**: 핵심 기능 모두 완성되어 즉시 사용 가능

**작동 방식**:
```
Claude 사용 시: Claude Code → Anthropic API (직접)
GLM 사용 시:   Claude Code → SwiftCast Proxy (localhost:32080) → GLM API
```

**구현 완료**:
- ✅ 프록시 서버 (OAuth 패스스루 + API 키 주입)
- ✅ 계정 관리 UI
- ✅ 사용량 모니터링 (모델별/일별/로그)
- ✅ 설정 페이지 (포트, 자동 시작)
- ✅ 토큰 사용량 자동 추적

**사용 방법**:
1. `npm run tauri dev` 실행
2. 원하는 계정 활성화 (Claude/GLM)
3. Claude Code 재시작
4. 사용량 실시간 확인!
